# Datatypes

# a = 10
# a = '''To insert characters that are illegal in a string, use an escape character.
# An escape character is a backslash followed by the character you want to insert.'''

a = None

print(a, type(a))